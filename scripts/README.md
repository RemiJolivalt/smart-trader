# Scripts — automation, cron, setup (placeholder)

> Scripts d'automatisation à venir : bootstrap dev, seeds DB, tâches cron locales, ops checklists.

Conventions :
- Scripts shell POSIX (`.sh`) ou PowerShell (`.ps1`) selon contexte Windows.
- Idempotents par défaut.
- Documentés en en-tête (usage, prérequis, exit codes).
